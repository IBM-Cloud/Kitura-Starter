applications:
- name: Kitura-Starter
  memory: 256M
  instances: 1
  random-route: true
  disk_quota: 1024M
  command: Kitura-Starter
  buildpack: swift_buildpack
  # Will re-add health-check lines once CF is upgraded within the Bluemix CLI
  # health-check-type: http
  # health-check-http-endpoint: /health
